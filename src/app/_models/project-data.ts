export class ProjectData {
  name: string
  graph: string
  code: string

  constructor(name: string, graph: string, code: string) {
    this.name = name
    this.graph = graph
    this.code = code
  }
}
